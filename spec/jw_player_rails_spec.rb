require 'spec_helper'

describe JwPlayerRails do
  it 'has a version number' do
    expect(JwPlayerRails::VERSION).not_to be nil
    expect(JwPlayerRails::JWPLAYER_VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
